# Kanban Plugin Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the `kanban` Shipyard plugin — `/kanban <PRD>` breaks a PRD into vertical-slice tickets and creates them on a real board (GitHub Issues or Jira).

**Architecture:** Thin `/kanban` command delegates to a `creating-tickets` skill that owns all backend-agnostic methodology (config/setup, slicing, approval gate, dedup, best-effort creation+summary). The skill never talks to a backend directly — it reads a backend-specific reference file (`references/github.md` or `references/jira.md`) for the exact commands/tool calls to use. GitHub goes through the `gh` CLI (reuses existing `gh auth`); Jira goes through an MCP server the plugin bundles itself (Atlassian's official remote MCP server, declared via `.mcp.json`, so the plugin has no dependency on any other marketplace plugin).

**Tech Stack:** Claude Code plugin system (plugin.json / commands / skills / bundled `.mcp.json`), `gh` CLI, Atlassian remote MCP server, git. No build step, no code dependencies.

## Global Constraints

- Repo root: the shipyard repository root (git repo on `main`, spec committed at `docs/superpowers/specs/2026-07-21-kanban-design.md`).
- Plugin name: `kanban`, kebab-case, matching the `prd` plugin's established layout.
- Do NOT set `version` in `plugin.json` or the marketplace plugin entry — git-sourced plugins auto-update per commit only when version is omitted (established in the `prd` plugin plan).
- Component dirs (`commands/`, `skills/`) live at plugin root, never inside `.claude-plugin/` — only manifest JSON goes there.
- Relative plugin `source` in `marketplace.json` must start with `./` and resolve against repo root.
- Owner identity: name "Jaxson Mansouri", email `mansouricobusiness@gmail.com`, GitHub `Jaxsonman`. License: MIT.
- Bundled Jira MCP server: name `atlassian`, `type: "http"`, `url: "https://mcp.atlassian.com/v1/mcp"` — this is Atlassian's official remote MCP server (verified against Atlassian's own docs and against the `atlassian-tools` third-party plugin already installed locally, which points at the identical URL). Per Claude Code's plugin MCP scoping rule, its tools appear as `mcp__plugin_kanban_atlassian__<toolName>` (e.g. `mcp__plugin_kanban_atlassian__createJiraIssue`).
- Canonical ticket body template (both backends use this verbatim, GitHub issue body and Jira description alike):

  ```
  ## Description

  <description>

  ## Acceptance Criteria

  1. <criterion 1>
  2. <criterion 2>

  ## How to verify

  <human verification step>

  ---
  Source PRD: <slug>
  ```

  The trailing `Source PRD: <slug>` line is the literal string both backend reference files search for when checking duplicates.
- No local ticket-tracking file is written — the board is the single source of truth (per spec Non-goals).
- Ticket creation is best-effort: attempt every approved ticket even if one fails; summarize successes (with links) and failures (with reason) at the end. Never halt the whole run on one failure.

---

### Task 1: Marketplace entry for `kanban`

**Files:**
- Modify: `.claude-plugin/marketplace.json`

**Interfaces:**
- Produces: marketplace `shipyard` exposing a plugin entry `{"name": "kanban", "source": "./plugins/kanban"}` (Task 2 must create that exact directory).

- [ ] **Step 1: Add the kanban entry to the marketplace manifest**

Modify `.claude-plugin/marketplace.json` — add a second entry to the `plugins` array (after the existing `prd` entry):

```json
{
  "name": "shipyard",
  "owner": {
    "name": "Jaxson Mansouri",
    "email": "mansouricobusiness@gmail.com"
  },
  "description": "SDLC pipeline plugins for Claude Code — from raw idea to shipped software.",
  "plugins": [
    {
      "name": "prd",
      "source": "./plugins/prd",
      "description": "Turn a raw idea into a structured PRD through a guided, one-question-at-a-time interview.",
      "category": "productivity",
      "keywords": ["prd", "requirements", "planning", "sdlc"]
    },
    {
      "name": "kanban",
      "source": "./plugins/kanban",
      "description": "Turn a PRD into vertical-slice tickets and create them on a real board (GitHub or Jira).",
      "category": "productivity",
      "keywords": ["kanban", "tickets", "sdlc", "github", "jira", "agile"]
    }
  ]
}
```

- [ ] **Step 2: Attempt validation**

Run: `claude plugin validate .`
Expected: PASS, or a warning that `./plugins/kanban` doesn't exist yet (acceptable at this point — the plugin directory is created in later tasks). A JSON/schema error is not acceptable — fix the JSON if so.

- [ ] **Step 3: Commit**

```bash
git add .claude-plugin/marketplace.json
git commit -m "feat: add kanban marketplace entry"
```

---

### Task 2: Plugin manifest + bundled Jira MCP server

**Files:**
- Create: `plugins/kanban/.claude-plugin/plugin.json`
- Create: `plugins/kanban/.mcp.json`

**Interfaces:**
- Consumes: marketplace entry from Task 1.
- Produces: plugin identity `kanban`; bundled MCP server `atlassian` whose tools appear at runtime as `mcp__plugin_kanban_atlassian__<toolName>` — Task 6 (Jira reference) depends on this exact scoped-name pattern.

- [ ] **Step 1: Write the plugin manifest**

Create `plugins/kanban/.claude-plugin/plugin.json` (no `version` field — see Global Constraints):

```json
{
  "name": "kanban",
  "description": "Turn a PRD into vertical-slice tickets and create them on a real board (GitHub or Jira).",
  "author": {
    "name": "Jaxson Mansouri",
    "email": "mansouricobusiness@gmail.com"
  },
  "repository": "https://github.com/Jaxsonman/shipyard",
  "license": "MIT",
  "mcpServers": "./.mcp.json",
  "keywords": ["kanban", "tickets", "sdlc", "github", "jira", "agile"]
}
```

- [ ] **Step 2: Bundle the Atlassian remote MCP server**

Create `plugins/kanban/.mcp.json`:

```json
{
  "atlassian": {
    "type": "http",
    "url": "https://mcp.atlassian.com/v1/mcp"
  }
}
```

- [ ] **Step 3: Validate the plugin manifest**

Run: `claude plugin validate ./plugins/kanban`
Expected: PASS for `plugin.json` and `.mcp.json` schema (commands/skills don't exist yet — that's fine at this step; re-run at the end of Task 5 for a full pass).

- [ ] **Step 4: Commit**

```bash
git add plugins/kanban/.claude-plugin/plugin.json plugins/kanban/.mcp.json
git commit -m "feat: add kanban plugin manifest and bundled Atlassian MCP server"
```

---

### Task 3: The `/kanban` command

**Files:**
- Create: `plugins/kanban/commands/kanban.md`

**Interfaces:**
- Consumes: nothing from earlier tasks (pure trigger).
- Produces: the `/kanban` command (namespaced `/kanban:kanban`), which hands off to `plugins/kanban/skills/creating-tickets/SKILL.md` (Task 4).

- [ ] **Step 1: Write the command**

Create `plugins/kanban/commands/kanban.md`:

```markdown
---
description: Break a PRD into vertical-slice tickets and create them on a real board
argument-hint: [PRD file path or pasted PRD text]
---

Read and follow `${CLAUDE_PLUGIN_ROOT}/skills/creating-tickets/SKILL.md`.

PRD input from the user (file path or raw text, may be empty): $ARGUMENTS

If the input is empty, ask the user for a PRD file path or to paste the PRD
text before proceeding. Otherwise, restate in one sentence what the PRD is
about to confirm you read it correctly, then begin the skill's process.
```

- [ ] **Step 2: Commit**

```bash
git add plugins/kanban/commands/kanban.md
git commit -m "feat: add /kanban command"
```

---

### Task 4: The `creating-tickets` skill — core methodology

**Files:**
- Create: `plugins/kanban/skills/creating-tickets/SKILL.md`

**Interfaces:**
- Consumes: `${CLAUDE_PLUGIN_ROOT}` resolves to the installed `kanban` plugin directory (standard Claude Code plugin variable); config file path `.claude/kanban.config.json` (project-relative, defined here); ticket body template (Global Constraints).
- Produces: the full backend-agnostic process (config/setup, slicing, approval gate, dedup, best-effort creation+summary). Delegates backend mechanics to `references/github.md` (Task 5) or `references/jira.md` (Task 6) by reading whichever file matches `config.backend`.

- [ ] **Step 1: Write the skill**

Create `plugins/kanban/skills/creating-tickets/SKILL.md`:

```markdown
---
name: creating-tickets
description: Breaks a PRD down into small, human-testable vertical-slice tickets and creates them on a real board (GitHub Issues or Jira). Use when the user wants to turn a PRD into tickets, create a kanban backlog, or runs /kanban.
---

# Creating Tickets

Turn a PRD into vertical-slice tickets, then create them on the user's
configured board. A vertical slice is a ticket that delivers one
user-observable outcome a human can click through and verify end-to-end —
even if it touches multiple layers (DB, API, UI) — as opposed to a
horizontal slice ("all the backend," "all the frontend") that nothing can
demo until every layer is done.

## Step 1: Config check

Look for `.claude/kanban.config.json` in the current project.

**If missing**, ask the user, one question at a time:

1. "Which board should tickets go to: GitHub or Jira?"
2. If GitHub: "Which repo? (owner/repo format)"
   If Jira: "Which Jira project key?"

Write the answer to `.claude/kanban.config.json`:

```json
{
  "backend": "github",
  "target": "owner/repo"
}
```

or

```json
{
  "backend": "jira",
  "target": "PROJECTKEY"
}
```

This file holds no secrets — auth is handled separately (`gh auth` for
GitHub, an OAuth prompt on first Jira tool call for Jira). Commit it:

```bash
git add .claude/kanban.config.json
git commit -m "chore: configure kanban board target"
```

**If present**, read it and skip straight to Step 2 — never re-prompt.

## Step 2: Read the PRD

The user's input is either a file path or raw pasted PRD text.

- If it looks like a file path, read that file.
- Otherwise, treat the input itself as the PRD text.

Derive a short kebab-case **slug** identifying this PRD:
- File path input: use the filename without extension (e.g.
  `docs/prd/2026-07-20-reef-tank.md` → `2026-07-20-reef-tank`).
- Raw text input: slugify the first heading or first line (e.g. "# Reef Tank
  Monitor" → `reef-tank-monitor`).

This slug is embedded in every ticket's `Source PRD:` line and is what Step
4's duplicate check searches for.

## Step 3: Propose the vertical-slice ticket breakdown

Decompose the PRD into vertical slices using judgment — there is no fixed
ratio of tickets to PRD sections. For each candidate slice, apply INVEST-style
criteria:

- **Independent** — demoable without waiting on a sibling slice, wherever
  the PRD's own dependencies allow it.
- **Small** — scoped to what a single AI dev agent can implement and a
  human can verify in one sitting. If a slice feels too big, split it along
  its next-smallest observable outcome, not along technical layers.
- **Testable** — has concrete, checkable acceptance criteria, not vague
  intent like "improve performance."

For each slice, write a ticket using this exact template:

```
## Description

<what this slice does and why, derived from the relevant PRD section(s)>

## Acceptance Criteria

1. <testable criterion>
2. <testable criterion>

## How to verify

<one explicit sentence describing exactly how a human confirms this slice
works end-to-end>

---
Source PRD: <slug from Step 2>
```

Present the **full list** to the user in one message: each ticket's title
plus a one-line summary (not the full body yet — that would be too long to
scan). Example:

```
Proposed tickets for <slug>:

1. User can create a water-parameter log entry
   Form + save endpoint + confirmation toast; verified by submitting a
   reading and seeing it in the log list.
2. User can view their log history
   ...
```

## Step 4: Duplicate check

Before creating anything, check whether tickets from this PRD already exist
on the board. Read `references/github.md` or `references/jira.md`
(whichever matches `config.backend`) for the exact search command, and
search for the literal string `Source PRD: <slug>`.

If any matches are found, tell the user what was found (titles + links) and
ask them to confirm before proceeding — do not silently skip or silently
create duplicates.

## Step 5: Approval gate

Wait for the user to approve the proposed list from Step 3, or to request
edits (add, remove, reword slices). Only proceed to Step 6 once they approve
the list as a whole. Do not create anything before this gate.

## Step 6: Create tickets (best-effort)

Read `references/github.md` or `references/jira.md` (whichever matches
`config.backend`) for the exact create command/tool call. Attempt every
approved ticket, in order, even if an earlier one fails — do not stop the
whole run on one failure.

For each ticket, record whether it succeeded (with its URL) or failed (with
the error message).

## Step 7: Summarize

Report the results as two lists:

```
Created (<n>):
- <title> — <url>
- <title> — <url>

Failed (<n>):
- <title> — <error reason>
```

If anything failed, suggest the user re-run `/kanban` after fixing the
underlying issue (e.g. re-authenticating) — re-running is safe because Step
4's duplicate check will catch tickets that already succeeded.
```

- [ ] **Step 2: Validate the skill's frontmatter**

Run: `claude plugin validate ./plugins/kanban`
Expected: PASS for the skill's YAML frontmatter (the `references/` files it
mentions don't exist yet — that's expected until Tasks 5–6; this step only
checks the skill file itself is well-formed).

- [ ] **Step 3: Commit**

```bash
git add plugins/kanban/skills/creating-tickets/SKILL.md
git commit -m "feat: add creating-tickets skill methodology"
```

---

### Task 5: GitHub backend reference

**Files:**
- Create: `plugins/kanban/skills/creating-tickets/references/github.md`

**Interfaces:**
- Consumes: `config.target` in `owner/repo` format (Task 4); ticket body template (Global Constraints).
- Produces: the exact `gh` CLI invocations Task 4's Step 4 (search) and Step 6 (create) read and follow when `config.backend == "github"`.

- [ ] **Step 1: Write the reference file**

Create `plugins/kanban/skills/creating-tickets/references/github.md`:

```markdown
# GitHub backend

Used when `config.backend` is `"github"`. `config.target` is `owner/repo`.

## Auth check

Before the first call in a session, verify auth:

```bash
gh auth status
```

If this fails, tell the user to run `gh auth login` and stop — do not
attempt to create or search issues without valid auth.

## Search for duplicates (Step 4 of SKILL.md)

```bash
gh issue list --repo <owner/repo> --search "Source PRD: <slug> in:body" --state all --json number,title,url
```

Parse the JSON array. If it's non-empty, those are the matches to report to
the user.

## Create a ticket (Step 6 of SKILL.md)

```bash
gh issue create --repo <owner/repo> --title "<title>" --body "<body>"
```

`<body>` is the full ticket template from SKILL.md Step 3 (Description /
Acceptance Criteria / How to verify / Source PRD line), passed as a single
shell argument — write it to a temp file and use `--body-file <path>` if the
body contains characters that are awkward to escape inline:

```bash
gh issue create --repo <owner/repo> --title "<title>" --body-file <temp-file-path>
```

On success, `gh issue create` prints the created issue's URL on stdout —
that is the URL to record for the Step 7 summary.

On failure (non-zero exit), capture stderr as the error reason for the Step
7 summary and continue to the next ticket.
```

- [ ] **Step 2: Commit**

```bash
git add plugins/kanban/skills/creating-tickets/references/github.md
git commit -m "feat: add GitHub backend reference for creating-tickets"
```

---

### Task 6: Jira backend reference

**Files:**
- Create: `plugins/kanban/skills/creating-tickets/references/jira.md`

**Interfaces:**
- Consumes: `config.target` as a Jira project key (Task 4); bundled MCP server tools scoped as `mcp__plugin_kanban_atlassian__<toolName>` (Task 2); ticket body template (Global Constraints).
- Produces: the exact MCP tool calls Task 4's Step 4 (search) and Step 6 (create) read and follow when `config.backend == "jira"`.

- [ ] **Step 1: Write the reference file**

Create `plugins/kanban/skills/creating-tickets/references/jira.md`:

```markdown
# Jira backend

Used when `config.backend` is `"jira"`. `config.target` is a Jira project
key (e.g. `PROJ`).

This plugin bundles its own Atlassian remote MCP server (see
`plugins/kanban/.mcp.json`), so no other plugin needs to be installed. Its
tools appear scoped as `mcp__plugin_kanban_atlassian__<toolName>`. The first
call in a session triggers a one-time OAuth prompt — if a tool call returns
an auth/consent error, tell the user to complete that prompt and retry.

## Search for duplicates (Step 4 of SKILL.md)

Call `mcp__plugin_kanban_atlassian__searchJiraIssuesUsingJql` with:

```json
{
  "jql": "project = \"<TARGET>\" AND text ~ \"Source PRD: <slug>\""
}
```

If the result's issue list is non-empty, those are the matches to report to
the user (use each issue's key + a `https://<their-site>.atlassian.net/browse/<key>`
link, or whatever URL shape the tool result returns — do not hardcode a
site hostname, read it from the tool's response).

## Create a ticket (Step 6 of SKILL.md)

Call `mcp__plugin_kanban_atlassian__createJiraIssue` with:

```json
{
  "projectKey": "<TARGET>",
  "issueType": "Story",
  "summary": "<title>",
  "description": "<body>"
}
```

`<body>` is the full ticket template from SKILL.md Step 3 (Description /
Acceptance Criteria / How to verify / Source PRD line).

On success, the tool result includes the created issue's key and/or URL —
record that for the Step 7 summary.

On failure, capture the tool's error message as the error reason for the
Step 7 summary and continue to the next ticket.
```

- [ ] **Step 2: Commit**

```bash
git add plugins/kanban/skills/creating-tickets/references/jira.md
git commit -m "feat: add Jira backend reference for creating-tickets"
```

---

### Task 7: Validate and local install verification

**Files:**
- No new files (fixes only, if verification fails).

**Interfaces:**
- Consumes: everything from Tasks 1–6.
- Produces: a verified-installable `kanban` plugin — the gate before flipping README status in Task 8.

- [ ] **Step 1: Full plugin validation**

Run: `claude plugin validate ./plugins/kanban`
Expected: PASS — manifest, bundled MCP config, command, and skill (with both
reference files now present) all valid.

Then: `claude plugin validate .`
Expected: PASS with no warnings about a missing `./plugins/kanban` path.

- [ ] **Step 2: Add the marketplace from the local path**

```bash
claude plugin marketplace add .
```
Expected: success message naming marketplace `shipyard` (or confirmation
it's already added, if Task 3/7 of the `prd` plan left it registered).

- [ ] **Step 3: Install the kanban plugin**

```bash
claude plugin install kanban@shipyard
```
Expected: install succeeds.

- [ ] **Step 4: Verify the command and skill landed**

```bash
find ~/.claude/plugins/cache -path "*kanban*/commands/kanban.md"
find ~/.claude/plugins/cache -path "*kanban*/skills/creating-tickets/SKILL.md"
find ~/.claude/plugins/cache -path "*kanban*/.mcp.json"
```
Expected: one path printed for each.

- [ ] **Step 5: Manual end-to-end check (GitHub)**

In a scratch project with a real (test) GitHub repo configured and `gh
auth status` already logged in:

1. Run `/kanban` with a short sample PRD (2-3 "Scope — in" bullets is
   enough).
2. Confirm the backend/target setup prompt appears exactly once and
   `.claude/kanban.config.json` is written with `"backend": "github"`.
3. Confirm the proposed ticket list is vertical-sliced (each ticket
   describes one human-testable outcome, not a layer).
4. Approve the list; confirm real GitHub issues are created with the
   `Source PRD:` line in each body.
5. Run `/kanban` again with the same PRD; confirm no setup prompt appears
   this time (config from step 2 is reused silently), and confirm the
   duplicate-check warning appears before the approval gate.

Expected: all six behaviors match. Fix and re-run this task if not.

- [ ] **Step 6: Manual end-to-end check (Jira)**

Repeat Step 5 against a real (test) Jira project, choosing `"backend":
"jira"` at setup. Confirm the first Jira tool call triggers the expected
OAuth prompt if not already authenticated, and that created issues appear
in the configured project with the `Source PRD:` line in their description.

Expected: matches. Fix and re-run this task if not.

- [ ] **Step 7: Manual partial-failure check**

Using either backend from Step 5/6, propose a ticket list where one ticket
is deliberately malformed so its create call fails (e.g. edit the approved
list to give one ticket an empty title before approving, if the tool allows
it, or temporarily point `config.target` at a repo/project you don't have
write access to for just that one ticket's attempt). Confirm:

1. The other tickets are still created despite the one failure (best-effort
   behavior from SKILL.md Step 6 — no early halt).
2. The Step 7 summary clearly lists which tickets succeeded (with links) and
   which failed (with the error reason).

Expected: matches. Fix and re-run this task if not.

If any step failed: fix the relevant file, re-run `claude plugin validate
.`, commit the fix as `fix: <what>`, and repeat this task from Step 1.

---

### Task 8: Update README

**Files:**
- Modify: `README.md`

**Interfaces:**
- Consumes: verified plugin from Task 7.
- Produces: public-facing docs reflecting `kanban` as available.

- [ ] **Step 1: Flip the pipeline stage table and add a usage example**

Modify `README.md`:

```markdown
## Pipeline stages

| Stage | Plugin | Status | Role |
|-------|--------|--------|------|
| 1 | `prd` | ✅ Available | Turn a raw idea into a structured PRD via guided interview |
| 2 | `kanban` | ✅ Available | Turn a PRD into tickets on a board |
| 3 | `dev-crew` | Planned | Dev agents pick up tickets and implement them |
| 4 | `qa` | Planned | Testing and review |
| 5 | `pr-flow` | Planned | PR creation and merge |
| 6 | `cicd` | Planned | Pipeline monitoring and deploy help |

## Usage

After installing `prd`, run:

```
/prd a mobile app that tracks reef tank water parameters
```

Claude interviews you one question at a time (problem, users, success metrics,
scope, requirements, risks) and writes the finished PRD to
`docs/prd/YYYY-MM-DD-<slug>.md` in your project.

After installing `kanban`, run:

```
/kanban docs/prd/2026-07-20-reef-tank.md
```

The first run in a project asks once which board to use (GitHub or Jira)
and where, then Claude proposes a full breakdown of small, vertical-slice
tickets — each one a single outcome a human can verify end-to-end. Approve
the list and Claude creates the tickets on your board.
```

Also update the `## Install` section to mention both plugins:

```markdown
## Install

```
/plugin marketplace add Jaxsonman/shipyard
/plugin install prd@shipyard
/plugin install kanban@shipyard
```
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: mark kanban plugin available in README"
```
