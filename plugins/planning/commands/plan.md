---
description: Engineer session — turn a spec'd ticket into an executable implementation plan
argument-hint: "[ticket — number, #N, key, or URL]"
---

Read and follow `${CLAUDE_PLUGIN_ROOT}/skills/planning-tickets/SKILL.md`.

Ticket reference from the user (may be empty): $ARGUMENTS

If empty, ask which ticket to plan (a ticket number, key, or URL) before
proceeding. Otherwise, begin the skill's process with that ticket.
