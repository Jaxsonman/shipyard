---
description: Guided PM/UX session — align on what a ticket really means and produce spec.md
argument-hint: "[ticket — number, #N, key, or URL]"
---

Read and follow `${CLAUDE_PLUGIN_ROOT}/skills/speccing-tickets/SKILL.md`.

Ticket reference from the user (may be empty): $ARGUMENTS

If empty, ask which ticket to spec (a ticket number, key, or URL) before
proceeding. Otherwise, begin the skill's process with that ticket.
